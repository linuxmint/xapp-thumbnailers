#!/usr/bin/python3

import os
import subprocess

import XappThumbnailers
t = XappThumbnailers.Thumbnailer()

subprocess.getoutput(f"dcraw -c -e -w '{t.args.input}' | convert -resize {t.args.size}x{t.args.size} - '{t.args.output}'")
